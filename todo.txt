
0. now also analytics worker is a cron ran at 3 am everyday. to save click bucket to db one call a day. to get the analytics of today. i want to not query the db instead get data from the redis. is my thinking going right. any possible tradeoffs? or failures during production ref: http://localhost:5000/cron


0.4 get country for analytics. device not setting up in analytics.


0. should i add circuit breakers? A circuit breaker around Mongo/Redis lets the service fail fast until dependencies recover.
2. add terms and policy
3. error pages: 404 500 etc.
5. Range-Based ID Generation  https://claude.ai/chat/18fa9123-b42c-4625-9c00-ea66cf79e380
6. normalise input: sushil@gmail.com = Sushil@gmail.com.
8. CI/CD
9. monitoring
12. some modularity
15. visual issues with the font and font color

bugs:
2. for logs: Vector Config,  Docker Compose  ref: https://claude.ai/chat/54b99c02-ca36-4d1c-936a-58d55cfdae68
4. logging out during email verification raises a bug.
5. dashboard if there is no link it is still clickable.  (top link, newest)
uniqueVisitors summed across days is an upper bound, not a true unique count. This is a known limitation of the daily-HLL-bucket design, not a bug — a visitor who clicks on both June 1 and June 2 gets counted twice in a 30-day summary. If you ever need a true unique-over-range count, you'd PFMERGE the daily HLL keys in Redis before they get flushed/deleted, rather than summing the post-flush MongoDB numbers. Worth a comment in the code so a future you doesn't assume it's exact — I left one in analytics.service.js


features for testing:
1. forgot password.
1. search is now working but testing is required.(new not searching).

arch Issues:
4. CORS origin is a single string — no multi-origin or regex support
APP_URL_CORS is a plain string. If you ever need staging + production origins, you'll need to refactor. Also, there's no fallback if the env var is missing — CORS will silently block all cross-origin requests with no error. Validate required env vars at startup.



