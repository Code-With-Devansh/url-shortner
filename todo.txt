
0.4 get country for analytics. device not setting up in analytics.


15. visual issues with the font and font color

bugs:
4. logging out during email verification raises a bug.
5. dashboard if there is no link it is still clickable.  (top link, newest)


features for testing:
1. forgot password.
1. search is now working but testing is required.(new not searching).

arch Issues:
4. CORS origin is a single string — no multi-origin or regex support
APP_URL_CORS is a plain string. If you ever need staging + production origins, you'll need to refactor. Also, there's no fallback if the env var is missing — CORS will silently block all cross-origin requests with no error. Validate required env vars at startup.



